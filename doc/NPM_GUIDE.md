# Руководство по NPM (Node Package Manager)

Этот документ — подробное руководство по работе с **npm**, основным инструментом управления зависимостями в проекте PDF3MD (для frontend части).

## 📦 Что такое npm?

**npm** (Node Package Manager) — это стандартный менеджер пакетов для Node.js. Он выполняет три основные функции:
1.  **Репозиторий**: Огромная онлайн-база данных публичных JavaScript-пакетов.
2.  **CLI (Командная строка)**: Инструмент для установки, удаления и управления этими пакетами.
3.  **Управление скриптами**: Запуск команд сборки, тестирования и разработки.

---

## 📄 Файл `package.json`

Это "паспорт" вашего проекта. Он находится в папке `pdf3md/` (frontend).

### Основные секции:

1.  **`scripts`**: Команды для автоматизации.
    ```json
    "scripts": {
      "dev": "vite",          // Запуск сервера разработки
      "build": "vite build",  // Сборка проекта для продакшена
      "preview": "vite preview" // Просмотр собранной версии
    }
    ```
    *Запуск:* `npm run dev` или `npm run build`.

2.  **`dependencies`** (Зависимости):
    Пакеты, необходимые для **работы** приложения в браузере (например, `react`, `react-dom`).
    *Команда установки:* `npm install <package_name>`

3.  **`devDependencies`** (Зависимости для разработки):
    Пакеты, нужные только **во время разработки** и сборки (например, `vite`, `eslint`). Они не попадают в финальный код, который видит пользователь.
    *Команда установки:* `npm install -D <package_name>`

---

## 🔒 Файл `package-lock.json`

**Это критически важный файл!**

*   **Что делает**: Фиксирует *точные* версии всех установленных пакетов (и их зависимостей).
*   **Зачем нужен**: Гарантирует, что у всех разработчиков и на всех серверах сборки (CI/CD) будет установлен **абсолютно одинаковый** набор пакетов.
*   **Правило**: Всегда коммитьте этот файл в Git. Никогда не удаляйте его просто так.

---

## 🛠️ Основные команды

Выполняйте эти команды, находясь в папке с `package.json` (в нашем случае `pdf3md/pdf3md/`).

### Установка пакетов

| Команда | Описание |
|---------|----------|
| `npm install` | Скачивает все пакеты, указанные в `package.json`. Выполнять после клонирования репо. |
| `npm i <пакет>` | Устанавливает пакет и добавляет его в `dependencies`. |
| `npm i -D <пакет>` | Устанавливает пакет как dev-зависимость (`devDependencies`). |
| `npm ci` | "Clean Install". Удаляет `node_modules` и ставит всё строго по `package-lock.json`. Идеально для чистой сборки. |

### Обновление и удаление

| Команда | Описание |
|---------|----------|
| `npm update` | Обновляет пакеты до последних версий (в рамках правил из package.json). |
| `npm uninstall <пакет>` | Удаляет пакет и убирает запись о нем из `package.json`. |

### Запуск скриптов

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запускает dev-сервер (в нашем случае Vite). |
| `npm run build` | Запускает скрипт сборки (Vite build). Создает папку `dist/`. |

---

## 🩹 Решение проблем (Troubleshooting)

Если frontend "ведет себя странно", возникают непонятные ошибки импорта или сборки:

### 1. "Ядерная очистка" (Nuclear Option)
Самый надежный способ исправить проблемы с зависимостями:

```bash
# Находясь в папке фронтенда
rm -rf node_modules
rm package-lock.json
npm install
```
*Это удалит все скачанные пакеты и файл блокировки, заставив npm пересчитать дерево зависимостей и скачать всё заново.*

### 2. Очистка кэша npm
Если даже переустановка не помогает (например, скачался "битый" пакет):

```bash
npm cache clean --force
```

### 3. Проверка уязвимостей
npm умеет проверять пакеты на известные дыры в безопасности:

```bash
npm audit
# Чтобы попробовать исправить автоматически:
npm audit fix
```

---

## 📚 Семантическое версионирование (SemVer)

В `package.json` вы увидите версии вроде `^18.2.0` или `~1.0.4`. Что значат эти символы?

*   `^1.2.3` (Карат): Разрешает обновление до **minor** версии (вторая цифра).
    *   Можно: `1.2.4`, `1.3.0`.
    *   Нельзя: `2.0.0` (Breaking changes).
*   `~1.2.3` (Тильда): Разрешает обновление только **patch** версии (третья цифра).
    *   Можно: `1.2.4`, `1.2.9`.
    *   Нельзя: `1.3.0`.
*   `1.2.3` (Без знака): Строгая версия. Установится только она.

---

## Итог для PDF3MD

В нашем проекте типичный рабочий процесс выглядит так:

1.  `cd pdf3md/pdf3md` — переходим в папку фронтенда.
2.  `npm install` — если вы только что скачали проект.
3.  `npm run dev` — пока пишем код.
4.  `npm run build` — когда готовы собирать DMG/EXE.
