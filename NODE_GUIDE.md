# Руководство по Node.js

Этот документ объясняет роль **Node.js** в проекте PDF3MD.

## 🟢 Что такое Node.js?

Node.js — это среда выполнения JavaScript, которая работает *вне* браузера (на вашем компьютере или сервере).

Если JavaScript в браузере управляет кнопками и анимацией, то Node.js позволяет JavaScript работать с файловой системой, сетью и процессами ОС.

### Роль в PDF3MD

В нашем проекте Node.js используется **ТОЛЬКО** для сборки Frontend части (React):

1.  **Инструментарий**: Node.js запускает `npm`, `vite`, и другие утилиты сборки.
2.  **Компиляция**: Он преобразует современный React/JSX код в стандартный JavaScript, понятный браузерам.
3.  **Dev Server**: Локальный сервер разработки (`npm run dev`) работает на Node.js.

> **Важно:** Сам backend (логика обработки PDF/DOCX) написан на **Python**. Node.js в финальном приложении (внутри DMG/EXE) **не нужен** для работы программы, он нужен только программисту для сборки.

---

## ⚙️ Версии Node.js

Мы используем LTS (Long Term Support) версии. На данный момент рекомендуется **v18** или **v20**.

Чтобы проверить вашу текущую версию:
```bash
node -v
```

### Управление версиями (nvm)

Чтобы не мучиться с установкой разных версий вручную, используйте **nvm** (Node Version Manager).

**Установка (macOS/Linux):**
```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
```

**Использование:**
```bash
nvm install 20      # Установить Node.js v20
nvm use 20          # Переключиться на v20
nvm alias default 20 # Сделать v20 версией по умолчанию
```

---

## 🏗️ Как это работает "под капотом"?

Когда вы пишете команду `npm run dev`:

1.  **Shell** находит `npm`.
2.  **npm** (написанный на Node.js) читает `package.json`.
3.  Он находит в папке `node_modules/.bin/` скрипт `vite`.
4.  Скрипт запускается через интерпретатор **node**.
5.  **Node.js** начинает выполнять JS-код Vite: слушать порты, читать файлы проекта, компилировать их на лету.

---

## 💀 CommonJS vs ES Modules (ESM)

В мире Node.js есть два стандарта модулей (импортов):

1.  **CommonJS** (Старый):
    ```javascript
    const React = require('react');
    module.exports = App;
    ```
    *Используется в старых проектах и некоторых скриптах конфигурации.*

2.  **ES Modules** (Новый, Стандарт):
    ```javascript
    import React from 'react';
    export default App;
    ```
    *Используется в нашем проекте (Vite, React).*

В файле `package.json` есть строчка `"type": "module"`, которая говорит Node.js использовать новый стандарт ESM по умолчанию для всех `.js` файлов.

---

## 🔧 Полезные команды Node REPL

Вы можете использовать Node.js как калькулятор или песочницу. Просто наберите `node` в терминале:

```javascript
> 2 + 2
4
> const fs = require('fs');
undefined
> fs.readdirSync('.'); // Показать файлы в текущей папке
[ 'README.md', 'package.json', ... ]
```
Нажмите `Ctrl+C` дважды, чтобы выйти.
